# selenium-api-tests
Common Selenium tests to verify either Cloud vs. stable APIs or APIs vs. stable Cloud.  Used in both `cloud` and all API projects.  To not tie as a submodule - can be checked out independently and locate apis and cloud project in the sibling directories.  CI of either Cloud or any API should checkout and run at the integration test step.
